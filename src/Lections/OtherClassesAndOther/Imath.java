package Lections.OtherClassesAndOther;

@FunctionalInterface// обычно используется для пометки интерфейса для лямбда-выражений, так как позволяет создать только один метод
public interface Imath {
    double summ(double a, double b);
}
